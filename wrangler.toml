name = "kaoshi"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
main = ".open-next/worker.js"

[assets]
directory = ".open-next/assets"

# Cloudflare Workers 配置
# 环境变量在 Workers Dashboard > Settings > Environment variables 中设置
